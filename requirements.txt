# 刻意只依赖 numpy。
#
# 不用 scipy —— 双二阶滤波器与 RBJ 设计公式都自己实现，这样 PC 上的结果
# 与 CMSIS-DSP 固件逐样本一致，避免"仿真好听、上机不对"。
# WAV 读写用标准库 wave 模块，不引入 soundfile。
numpy>=1.24

# 调参界面所需（命令行工具不需要）
sounddevice>=0.4    # 实时音频回放
matplotlib>=3.7     # 频响曲线

# 采集固件配套（tools/capture.py 需要，其余工具不需要）
pyserial>=3.5      # 串口接收
